# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets - never commit (keys, credentials, PII)
.env
.env.*
!.env.example
*-key.json
*-credentials.json
service-account*.json

# Local sample data that may contain real PII
sample_data/real/

# Tooling caches
.pytest_cache/
.ruff_cache/

# ADK local eval history (written by `adk eval` runs)
.adk/

# Agent-facing working files - kept local, not part of the portfolio repo
HANDOFF.md
CLAUDE.md
